<Window x:Class="WpfApp.RegisterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Library Elite - Đăng Ký Tài Khoản" Height="550" Width="450"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="#2C3E50">
    <Grid>
        <Border Background="White" CornerRadius="10" Margin="30" Padding="25"
                VerticalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.3"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock Text="📝 ĐĂNG KÝ TÀI KHOẢN" FontSize="22" FontWeight="Bold" 
                           Foreground="#2C3E50" HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <TextBlock Text="Tên đăng nhập *" FontSize="12" Foreground="#7F8C8D" Margin="0,0,0,3"/>
                <TextBox x:Name="txtUsername" FontSize="13" Padding="8,6" Margin="0,0,0,10"
                         BorderBrush="#BDC3C7" BorderThickness="1"/>

                <TextBlock Text="Mật khẩu *" FontSize="12" Foreground="#7F8C8D" Margin="0,0,0,3"/>
                <PasswordBox x:Name="txtPassword" FontSize="13" Padding="8,6" Margin="0,0,0,10"
                             BorderBrush="#BDC3C7" BorderThickness="1"/>

                <TextBlock Text="Họ và tên *" FontSize="12" Foreground="#7F8C8D" Margin="0,0,0,3"/>
                <TextBox x:Name="txtFullName" FontSize="13" Padding="8,6" Margin="0,0,0,10"
                         BorderBrush="#BDC3C7" BorderThickness="1"/>

                <TextBlock Text="Email *" FontSize="12" Foreground="#7F8C8D" Margin="0,0,0,3"/>
                <TextBox x:Name="txtEmail" FontSize="13" Padding="8,6" Margin="0,0,0,10"
                         BorderBrush="#BDC3C7" BorderThickness="1"/>

                <TextBlock Text="Số điện thoại" FontSize="12" Foreground="#7F8C8D" Margin="0,0,0,3"/>
                <TextBox x:Name="txtPhone" FontSize="13" Padding="8,6" Margin="0,0,0,10"
                         BorderBrush="#BDC3C7" BorderThickness="1"/>

                <TextBlock Text="Địa chỉ" FontSize="12" Foreground="#7F8C8D" Margin="0,0,0,3"/>
                <TextBox x:Name="txtAddress" FontSize="13" Padding="8,6" Margin="0,0,0,15"
                         BorderBrush="#BDC3C7" BorderThickness="1"/>

                <Button Content="ĐĂNG KÝ" Click="btnRegister_Click" Height="38" FontSize="14"
                        FontWeight="Bold" Background="#27AE60" Foreground="White" 
                        BorderThickness="0" Cursor="Hand" Margin="0,0,0,8"/>

                <Button Content="Quay lại đăng nhập" Click="btnBack_Click"
                        Background="Transparent" Foreground="#3498DB" BorderThickness="0"
                        Cursor="Hand" FontSize="12"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
